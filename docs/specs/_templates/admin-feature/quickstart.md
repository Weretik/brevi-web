# Quickstart: <назва Admin feature>

## Автоматична перевірка

```text
npx nx lint <project>
npx nx test <project>
npx nx build admin
```

## Ручна перевірка

1. <P1 user story — дія та очікуваний результат>
2. <loading / empty / error / forbidden state>
3. <keyboard, focus, responsive breakpoint>
4. <role / 401 / 403 / session expiry або n/a з причиною>
5. <перевірка, що PII, tokens і raw API errors не відображаються та не логуються, якщо застосовно>
